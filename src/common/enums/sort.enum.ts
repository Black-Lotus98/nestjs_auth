export enum SortEnum {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum SortByEnum {
  id = 'id',
  firstName = 'firstName',
  lastName = 'lastName',
  email = 'email',
  phone = 'phone',
  createdAt = 'createdAt',
  updatedAt = 'updatedAt',
}
